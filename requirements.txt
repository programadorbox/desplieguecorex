# Servidor de aplicaciones (Producción)
gunicorn==23.0.0

# Framework y base de datos (Adaptable a PostgreSQL en producción)
django>=4.2,<5.0
psycopg2-binary==2.9.9

# Gestión de configuración y seguridad
python-dotenv==1.0.1
whitenoise==6.6.0

# Opcional: Para manejar archivos estáticos en nube (ej. AWS S3)
django-storages[boto3]==1.14.2
